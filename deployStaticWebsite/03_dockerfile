# using Dockerfile for building static website
1. move to folder with static content and copy Dockerfile here
docker build -t widgetfactory:1.0 .

2. see image layers and size bind env's vars
export showLayers='{{ range .RootFS.Layers }}{{ println . }}{{ end }}'
export showSize='{{ .Size }}'
docker inspect -f "$showSize" <image-name>

3. run container with static content
docker run --name web1 -p 8080:80 widgetfactory:1.0